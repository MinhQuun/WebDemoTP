1. Chạy migration và seeder:
php artisan migrate --seed

2. Xóa toàn bộ bảng (Drop) -> Chạy lại Migration
php artisan migrate:fresh --seed

3. Chạy web:
php artisan serve

4. Thực thi lệnh composer install

    4.1. Mở: C:\xampp\php
    4.2. Kiếm các file có tên: php.ini-development, php.ini-production
    4.3. Mở bằng notepad
    4.4. Tìm và bỏ dấu ; các lệnh sau đây:
        - extension=zip
        - extension=fileinfo
        - extension=mbstring
        - extension=openssl
        - extension=intl       ; (không bắt buộc, nhưng Laravel hay dùng)
        - extension=bcmath     ; (một số package cần)

# Clear cache (nếu lỗi view/config)
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear

php artisan optimize:clear


1. Sửa .env
DB_CONNECTION=sqlsrv
DB_HOST=127.0.0.1
DB_PORT=1433
DB_DATABASE=TPBS
DB_USERNAME=laravel_user
DB_PASSWORD=123456

2. Clear config cache (quan trọng)
php artisan config:clear
php artisan cache:clear

(Nếu bạn từng chạy config:cache thì chạy thêm:)
php artisan config:cache

3. Test bằng Tinker
php artisan tinker
>>> DB::select("SELECT 1 AS ok");

Nếu ra ok = 1 ✅ là Laravel đã kết nối được.

Hoặc test lấy bảng:
DB::select("SELECT TOP 1 * FROM ten_bang");

